import BookClient from './BookClient';

export const metadata = {
    title: 'Book a Consultation - Sheen Agency',
    description: 'Schedule a free consultation with Sheen Agency experts.',
};

export default function BookPage() {
    return <BookClient />;
}
